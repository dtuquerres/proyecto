import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CollarPage } from './collar';

@NgModule({
  declarations: [
    CollarPage,
  ],
  imports: [
    IonicPageModule.forChild(CollarPage),
  ],
})
export class CollarPageModule {}
