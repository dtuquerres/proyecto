import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AbrigoPage } from './abrigo';

@NgModule({
  declarations: [
    AbrigoPage,
  ],
  imports: [
    IonicPageModule.forChild(AbrigoPage),
  ],
})
export class AbrigoPageModule {}
